<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.sun.mvc.dao.UserAccountDao">
    
    <select id="queryUserAccountTotal"  resultType="int">
		select count(*) from t_user_account
	</select>
	
	<select id="queryUserAccountList" parameterType="map" resultType="hashmap">
		select bus_no,fee from t_user_account
	</select>
	
	<select id="queryUserAccount" parameterType="map" resultType="hashmap">
		select bus_no,fee from t_user_account
	</select>
</mapper>  